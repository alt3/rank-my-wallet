databases:
  - name: rank-my-wallet-db
    plan: free

services:
  - type: web
    name: rank-my-wallet
    env: node
    plan: free
    buildCommand: yarn --frozen-lockfile --prod=false && blitz build && blitz prisma migrate deploy
    startCommand: blitz start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: rank-my-wallet-db
          property: connectionString
      - key: SESSION_SECRET_KEY
        generateValue: true
